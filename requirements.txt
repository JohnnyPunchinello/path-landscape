networkx>=3.0
numpy>=1.23,<2
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.6
# optional: needed only for path_landscape.torch_bridge and demo_torch.py
torch>=2.0
# optional: needed only for persistence_h1 (dim-1 persistent homology)
ripser>=0.6
# optional: needed only for path_landscape.agent (LLM-powered analysis pipeline)
anthropic>=0.40
# optional: needed only for path_landscape.webapp (browser frontend)
flask>=3.0
markdown>=3.5
